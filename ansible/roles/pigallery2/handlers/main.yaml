# pigallery
---

- name: 
  ansible.builtin.import_tasks: global-handlers/main.yaml

- name: Restart pigallery
  listen: "restart pigallery"
  ansible.builtin.systemd:
    name: "pigallery.service"
    state: "restarted"
  become: true
